# Production build config — VITE_API_URL is baked into the bundle at `npm run build`.
# This is the deployed Railway backend's public URL (no trailing slash).
# The dashboard derives the WebSocket URL automatically (https -> wss).
VITE_API_URL=https://fidel-production-39d9.up.railway.app
